[project]
name = "log-ai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "drain3>=0.9.11",
    "litellm>=1.95.0",
    "python-dotenv>=1.2.2",
]

[project.scripts]
log-ai = "log_ai:cli:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
